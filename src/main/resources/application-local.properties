# =============================================================
# Perfil: local
# Uso: entorno de desarrollo con Docker Compose
# Activar: SPRING_PROFILES_ACTIVE=local
#
# La variable DB_HOST es inyectada por docker-compose.yml
# con el valor "postgres" (nombre del servicio PostgreSQL).
# En desarrollo local sin Docker, DB_HOST=localhost por defecto.
# =============================================================

# JPA - desarrollo
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Logging
logging.level.eci.edu.co.Tech_Cup_DOSW_BackEnd_2026_1=DEBUG
